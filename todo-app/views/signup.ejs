<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <title> <%= title %></title>
    <link rel="stylesheet" href="./css/styles.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Include Tailwind CSS (You might want to use a specific version instead of a CDN in a production environment) -->
    
</head>
<body>
    <div class="grid grid-cols-6 py-2 text-slate-950">
        <div class="col-start-3 col-span-2 text-slate-950">
            <!-- Include your header.ejs content here -->
            <%- include("header.ejs") %> 
            <h6 class="py-4">Signup as a new user</h6>
            <form action="/users" method="post">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <div class="py-3">
                    <label for="firstName" class="block text-sm font-medium text-gray-600">First Name</label>
                    <input type="text" name="firstName" id="firstName" class="border border-gray-300 text-gray-900 rounded p-2 w-full" required/>
                </div>
                <div class="py-3">
                    <label for="lastName" class="block text-sm font-medium text-gray-600">Last Name</label>
                    <input type="text" name="lastName" id="lastName" class="border border-gray-300 text-gray-900 rounded p-2 w-full">
                </div>
                <div class="py-3">
                    <label for="email" class="block text-sm font-medium text-gray-600">Email</label>
                    <input type="email" name="email" id="email" class="border border-gray-300 text-gray-900 rounded p-2 w-full" required/>
                </div>
                <div class="py-3">
                    <label for="password" class="block text-sm font-medium text-gray-600">Password</label>
                    <input type="password" name="password" id="password" class="border border-gray-300 text-gray-900 rounded p-2 w-full" required/>
                </div>
                <div class="py-3">
                <button type="submit" class="bg-green-500 text-white px-5 py-2 rounded hover:bg-green-800 font-medium">Sign up</button>
                </div>
            </form>
            <% if(messages && messages.error && messages.error.length > 0)  { %>
                <% for(var i=0; i<messages.error.length; i++) { %>
                 <p ><%= messages.error[i] %></p>
                <% } %>
              <% } %>
        </div>
    </div>
</body>
</html>
